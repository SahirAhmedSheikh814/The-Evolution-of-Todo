apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "todo-web-app.fullname" . }}-test-connection"
  labels:
    {{- include "todo-web-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "todo-web-app.fullname" . }}-frontend:{{ .Values.frontend.service.port }}']
  restartPolicy: Never
